/** Target of the skip-to-content link. Keep in English — it is a DOM id, not UI copy. */
export const MAIN_CONTENT_ID = "main-content";

/** Landing hero — the header stays clear over this section, then frosts after 80px. */
export const HERO_SECTION_ID = "hero";
